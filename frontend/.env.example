# Local dev: defaults live in .env.development (auto-loaded by Vite).
# Optional overrides: copy these lines to .env.local (gitignored).
#
# Render production: set both in the Render dashboard, then Manual Deploy.

VITE_API_URL=http://localhost:8000
VITE_PUBLIC_MCP_URL=http://localhost:8080/mcp
